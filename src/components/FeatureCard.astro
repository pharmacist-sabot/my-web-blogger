---
import { Icon } from 'astro-icon/components';

interface Props {
  title: string;
  icon: string;
}

const { title, icon } = Astro.props;
---
<div class="card">
  <div class="card-header">
    <Icon name={`heroicons:${icon}-solid`} class="card-icon" />
    <h3>{title}</h3>
  </div>
  <div class="card-content">
    <slot />
  </div>
</div>

<style>
  .card {
    background-color: var(--card-bg-color);
    border: 1px solid var(--card-border-color);
    border-radius: 12px;
    padding: 1.5rem;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
    /* ตั้งค่าสีตัวอักษรสำหรับเนื้อหาใน card ให้ใช้ตัวแปรกลาง */
    color: var(--font-color); 
    transition: background-color 0.2s ease-in-out, border-color 0.2s ease-in-out;
  }

  .card-header {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 1rem;
  }
  
  .card-icon {
    /* ใช้สี Brand ที่คุณกำหนดไว้ */
    color: var(--brand-color);
    width: 24px;
    height: 24px;
  }

  h3 {
    margin: 0;
    font-size: 1.1rem;
    font-weight: 600;
    /* ใช้สี Brand เช่นเดียวกับ icon */
    color: var(--brand-color);
  }

  .card-content {
    line-height: 1.6;
    /* ลดความเข้มของสีเนื้อหาเล็กน้อยเพื่อให้ Title เด่นขึ้น */
    opacity: 0.9;
  }

  /* ทำให้ Link ภายใน Card มีสีที่ถูกต้อง */
  .card-content :global(a) {
    color: var(--brand-color);
    text-decoration: underline;
  }
</style>